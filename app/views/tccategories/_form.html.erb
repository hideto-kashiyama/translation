<%= form_for(@tccategory) do |f| %>
  <% if @tccategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tccategory.errors.count, "error") %> prohibited this tccategory from being saved:</h2>

      <ul>
      <% @tccategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ctg %><br>
    <%= f.text_field :ctg %>
  </div>
  <div class="field">
    <%= f.label :bunrui_id %><br>
    <%= f.number_field :bunrui_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
